
// pageFixture.ts
import { Page } from '@playwright/test';

export const pageFixture = {
    // Cleaner TypeScript definition without using "unknown" forcing
    page: undefined as unknown as Page 
};
